/**
 * Учётные записи админов (для file:// или как запасной вариант).
 * В публичном репозитории список пустой — добавьте логины только локально и не публикуйте пароли.
 * Через HTTP можно использовать admin_users.json (файл в .gitignore).
 */
window.FinTatAdminUsers = [];
